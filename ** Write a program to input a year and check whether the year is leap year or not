#include <stdio.h>

int main() {
    // Write C code here
    int year;
    printf("enter a year");
    scanf("%d",&year);
    if(year%100==0)
    { 
        if(year%400==0)
        {
            
        
        printf("the year is leap");
    }
    else 
    {
        printf("not a leap year");
    }
    }
    else
   if (year%4==0)
   {
    printf("the year is leap");
    }
    else
    {
    printf("not a leap year");
    }

    return 0;
}

