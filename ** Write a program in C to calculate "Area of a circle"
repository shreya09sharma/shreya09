#include <stdio.h>

int main() {
    float pi,area,r;
    printf("\n enter the value of pi");
    scanf("%f",&pi);
    printf("\n enter the value of radius");
    scanf("%f",&r);
    area=3.14*r*r;
    printf("\n enter the area %f\n",area);

    return 0;
}
