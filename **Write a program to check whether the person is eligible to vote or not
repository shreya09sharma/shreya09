#include <stdio.h>

int main() {
    // Write C code here
    int age;
    printf("enter the age");
    scanf("%d",&age);
    if(age>=18)
    {
        printf("the person is eligible to vote")
    }
else 
{
    printf("the person is not eligible to vote");
}
    return 0;
}
